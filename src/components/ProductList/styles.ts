import styled from 'styled-components'

export const Container = styled.div`
  margin-top: 2rem;

  ul {
    list-style: none;

    li {
      margin-top: 1rem;

      padding: 1rem;
      border-radius: 2rem;
      color: var(--grey);
      background: var(--white);
    }
  }
`
